<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyCard" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/colorPrimary_black</item>
        <item name="android:background">@drawable/back_black</item>
        <item name="android:textColor">@color/colorPrimary_white</item>
    </style>
    <style name="Theme.MyCard" parent="Base.Theme.MyCard" />
</resources>